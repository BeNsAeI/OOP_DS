#include <iostream>
#include <stdlib.h>
#include <stdio.h>
#include <string>
#include <string.h>
#include <cstring>
#include "color.h"
#include "Data.h"
#include "Node.h"
#include "linkedList.h"
LinkedList::LinkedList(){
}
void LinkedList::pushQueue(struct Node * node){
}
void LinkedList::popQueue(struct Node * node){
}
void LinkedList::pushStack(struct Node * node){
}
void LinkedList::popStack(struct Node * node){
}
void LinkedList::addAfter(struct Node * current, struct Node * newNode){
}
void LinkedList::addBefore(struct Node * current, struct Node * newNode){
}
void LinkedList::remove(struct Node * node){
}
struct Node * search(int value){
}
struct Node * search(float value){
}
struct Node * search(char value){
}
struct Node * search(std::string value){
}
LinkedList::~LinkedList{
}
